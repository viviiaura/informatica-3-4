#include <stdio.h>

int main(void){
    int number; // I fixed the variable by deleting the exclamation mark
    int test_value; // I added an underscore between test and value
    int example1; // I moved the 1 behind example
    float maria; // I deleted the int and named it
printf("Variables created successfully.\n");
}
